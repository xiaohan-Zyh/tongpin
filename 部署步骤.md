# 同频 —— Vercel 部署操作清单

> 项目路径：`D:\kanshanAI\zhihu-hot-app\`
> 本文件是照着做就行的操作步骤，不需要懂代码。

---

## 准备：你需要先拿到 3 样东西

| 需要的东西 | 在哪拿 | 当前状态 |
|---|---|---|
| GitHub 账号 | https://github.com 注册 | 待确认 |
| Vercel 账号 | https://vercel.com 用 GitHub 账号直接登录 | 待确认 |
| 知乎 Access Secret | https://developer.zhihu.com/profile | ⚠️ **缺失，见下方说明** |

### ⚠️ 关于 Access Secret

它和赛事给的 App Key 是**两个不同的东西**：

- **App ID / App Key**（`471` / `4c0b...`）：赛事页面分配，用于**用户登录**。已有。
- **Access Secret**：知乎开放平台个人中心的密钥，用于**读取热榜、我的创作列表**。目前缺失。

它之前保存在你电脑的系统凭证库里，但只能读到掩码、读不出明文，所以无法自动填入。

**去哪拿**：登录 https://developer.zhihu.com/profile ，复制页面上的 Access Secret。

**不填会怎样**：登录、发观点、观点匹配、对话**都能正常用**（这些是作品核心功能），
只有首页热榜会显示一句配置提示。建议填上，让 Demo 完整。

---

## 第一步：把代码传到 GitHub

### 1.1 创建空仓库

打开 https://github.com/new ，填写：

- **Repository name**：`tongpin`（或你喜欢的名字）
- **Public / Private**：都可以，Vercel 都能读取
- ⚠️ **下面三个勾全部不要勾**：Add a README file、Add .gitignore、Choose a license
  （勾了会导致仓库非空，推送时冲突）

点 **Create repository**。

### 1.2 复制仓库地址

创建完成后，页面会显示一个地址，形如：

```
https://github.com/你的用户名/tongpin.git
```

**把这个地址发给我，我来执行推送。**

> 如果你想自己推送，在项目目录下依次执行：
> ```bash
> git remote add origin https://github.com/你的用户名/tongpin.git
> git push -u origin master
> ```

---

## 第二步：在 Vercel 部署

### 2.1 导入项目

1. 打开 https://vercel.com ，点 **Continue with GitHub** 登录
2. 进入后点右上角 **Add New...** → **Project**
3. 在仓库列表里找到刚才创建的 `tongpin`，点它右边的 **Import**
   - 如果列不出来，点 **Adjust GitHub App Permissions** 授权 Vercel 访问你的仓库

### 2.2 配置环境变量（关键一步）

导入后会进入配置页，**先不要急着点 Deploy**。

展开 **Environment Variables** 区域，逐条添加下面 5 个变量
（左边填 Key，右边填 Value，每填一条点一次 **Add**）：

| Key | Value |
|---|---|
| `ZHIHU_OAUTH_APP_ID` | `471` |
| `ZHIHU_OAUTH_APP_KEY` | 见本地 `.env.local` 文件中该项的值 |
| `SESSION_SECRET` | `soOdCMIvnR3I9i-aUvxCJ2q0GwZaVCGO-jDBvA0djqI` |
| `ZHIHU_ACCESS_SECRET` | 从知乎开放平台个人中心复制 |
| `ZHIHU_OAUTH_REDIRECT_URI` | **先随便填** `https://placeholder.vercel.app/api/auth/callback`，第三步再改 |

> `ZHIHU_OAUTH_APP_KEY` 的真实值在 `D:\kanshanAI\zhihu-hot-app\.env.local` 里，
> 用记事本打开即可看到。此处不写明文是为了避免密钥扩散。

### 2.3 部署

点 **Deploy**，等待 1~3 分钟。

看到 🎉 撒花动画就是成功了。

---

## 第三步：拿到域名并登记回调地址

### 3.1 域名在哪里

部署成功页面上会直接显示域名，形如：

```
tongpin-xxxx.vercel.app
```

也可以在项目 **Dashboard** 首页顶部的 **Domains** 区域看到。
这个域名是 Vercel 自动分配的，**不需要你买域名**，自带 HTTPS。

### 3.2 用这个域名拼出回调地址

在域名后面加上 `/api/auth/callback`，例如：

```
https://tongpin-xxxx.vercel.app/api/auth/callback
```

⚠️ 必须是 `https://` 开头，结尾**不要加斜杠**。

### 3.3 回填到 Vercel

1. 进入项目 → **Settings** → **Environment Variables**
2. 找到 `ZHIHU_OAUTH_REDIRECT_URI`，点右边 **...** → **Edit**
3. 把值改成上一步拼出的真实地址，保存

### 3.4 到赛事页面登记同一个地址

到知乎黑客松赛事页面，把**完全相同**的地址填进回调地址/redirect_uri 一栏。

⚠️ 两边必须**逐字符一致**：协议、域名、路径、大小写、尾斜杠都不能差。
差一个字符，登录就会失败。

### 3.5 重新部署（必做）

Vercel 改环境变量**不会自动生效**，必须重新部署一次：

进入 **Deployments** 标签 → 最上面那条记录右边 **...** → **Redeploy** → 确认。

---

## 第四步：验收

打开你的域名，依次确认：

- [ ] 首页能打开，热榜有内容（若提示缺 Access Secret，回第二步补上）
- [ ] 点「登录」能跳到知乎授权页，授权后能跳回来并显示你的昵称
- [ ] 进「同频」页面，发一条观点，能看到匹配结果和相似度百分比
- [ ] 在「广场」对别人的观点发起交流

**把域名发给我，我可以帮你远程验证这些链路。**

---

## 已知问题（评委可能遇到，已在产品文档中说明）

Vercel 是多实例架构，实例会被回收，而本项目数据存在进程内存里。因此：

- 新发表的观点**可能刷新后消失**
- 交流请求、对话**可能时有时无**

但 8 条种子观点在每个实例启动时都会写入，所以**匹配功能的演示效果始终正常**
——登录后发一条观点，一定能看到相似度和匹配理由。

如需彻底解决，可接入 Vercel KV（Redis），改动集中在
`src/lib/store.ts` 和 `src/lib/state-store.ts` 两个文件。
